# 🎓 UCC - Variables de Entorno de Ejemplo
# Copiar este archivo a .env y ajustar los valores según sea necesario

# =============================================================================
# CONFIGURACIÓN DE BASE DE DATOS
# =============================================================================

# MongoDB Configuration
MONGODB_URI=mongodb://appuser:apppass@localhost:27017/app?authSource=app&retryWrites=true&w=majority
MONGODB_DB=app
MONGODB_COLLECTION=items

# MongoDB Docker Configuration (para docker-compose)
MONGO_INITDB_ROOT_USERNAME=root
MONGO_INITDB_ROOT_PASSWORD=secret
MONGO_INITDB_DATABASE=app

# =============================================================================
# CONFIGURACIÓN DE CACHE
# =============================================================================

# Memcached Configuration
MEMCACHED_ADDR=localhost:11211
CACHE_TTL_SECONDS=300

# Redis Configuration (alternativa a Memcached)
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=
REDIS_DB=0

# =============================================================================
# CONFIGURACIÓN DE APLICACIÓN
# =============================================================================

# Server Configuration
PORT=8080
HOST=localhost
API_BASE_URL=http://localhost:8080

# Environment
ENV=development
DEBUG=true
LOG_LEVEL=debug

# =============================================================================
# CONFIGURACIÓN DE SEGURIDAD
# =============================================================================

# JWT Configuration
JWT_SECRET=tu-clave-secreta-muy-segura-cambiar-en-produccion
JWT_EXPIRATION_HOURS=24

# API Keys (si se usan)
API_KEY=tu-api-key-aqui

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080
CORS_ALLOWED_METHODS=GET,POST,PUT,DELETE,OPTIONS
CORS_ALLOWED_HEADERS=Content-Type,Authorization

# =============================================================================
# CONFIGURACIÓN DE SERVICIOS EXTERNOS
# =============================================================================

# Email Service (ejemplo con SendGrid)
SENDGRID_API_KEY=tu-sendgrid-api-key
FROM_EMAIL=noreply@tudominio.com

# AWS Configuration (si se usa)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=tu-access-key
AWS_SECRET_ACCESS_KEY=tu-secret-key
S3_BUCKET=tu-bucket-name

# =============================================================================
# CONFIGURACIÓN DE DESARROLLO
# =============================================================================

# Hot Reload (para Air)
AIR_BUILD_CMD=go build -o ./tmp/main
AIR_BUILD_DIR=tmp
AIR_INCLUDE_EXT=go,tpl,tmpl,html
AIR_EXCLUDE_DIR=assets,tmp,vendor,testdata

# Testing
TEST_DATABASE_URL=mongodb://localhost:27017/app_test

# =============================================================================
# NOTAS IMPORTANTES
# =============================================================================

# 1. NUNCA commitear el archivo .env a Git
# 2. Cambiar valores por defecto en producción
# 3. Usar contraseñas fuertes para bases de datos
# 4. Rotar claves y secrets regularmente
# 5. Verificar que todos los servicios externos estén configurados